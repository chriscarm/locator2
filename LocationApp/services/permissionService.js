
// Mock permission requests for the app
export function requestLocationPermission() {
    return Promise.resolve(true); // Mocked as always granted
}
export function requestContactsPermission() {
    return Promise.resolve(true); // Mocked as always granted
}
